/**
 * 매직 리터럴 바꾸기
 * 
 * 의미없는 9.81같은 숫자는 상수처리하는것이 좋다.
 */
const STANDARD_GRAVITY = 9.81;
function potentialEnergy(mass, height) {
  return mass * STANDARD_GRAVITY * height;
}

const MAX_ITEMS = 5;
item * MAX_ITEMS;
